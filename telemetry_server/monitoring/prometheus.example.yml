global:
  scrape_interval: 15s
  evaluation_interval: 30s

rule_files:
  - /etc/prometheus/alerts/telemetry-alerts.yml

scrape_configs:
  - job_name: telemetry-api
    metrics_path: /metrics
    static_configs:
      - targets:
          - telemetry-api:8000


Responsive Forms/Learning/Use `flex-col sm:flex-row` and `w-full sm:w-auto` for form inputs to ensure they stack correctly on mobile while maintaining a horizontal layout on desktop.
Icon-Only Buttons/Learning/Icon-only buttons must have an 'aria-label' attribute to be accessible to screen readers, as 'title' is not reliably announced./Action/Ensure all button elements without visible text content have a descriptive 'aria-label'.
